product_status = 'closed'

unless product_status == 'open' #if not também funciona
    check_change = 'can'
else
    check_change = 'can not'
end

puts "you #{check_change} change the broduct"